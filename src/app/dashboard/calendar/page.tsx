import Calendar from "@/sections/Calendar";

export const metadata = {
  title: "Calendar - Dashboard",
  description: "View and manage your events in the calendar",
};

export default function CalendarPage() {
  return <Calendar />;
}
